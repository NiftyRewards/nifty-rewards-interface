import { Button, Heading, VStack, Text } from "@chakra-ui/react";

const Home = () => {
  return (
    <VStack>
      <Heading>Heading</Heading>
      <Text>Text</Text>
      <Button>Button</Button>
    </VStack>
  );
};

export default Home;
